Liste des choses à faire:

interface des mods et affichage de la fusée en html
	menu
		generer les fichiers listft...
		afficher la liste
		modifier les mods
	trouver les paramètres idéaux pour une moyenne de nbgen=100 optimale
		nbpop, nbmut, ratiokill (peut etre les % de mutation)
		faire un script qui construit le define
		faire un script qui calcule l'intervalle de confiance

changer le script create engine pour avoir cut -f1,2,3 -d " "